#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Load config files, if any.

    // Run command loop.
    // lsh_loop();
    // Clear stdin

    // Perform any shutdown/cleanup.
    return EXIT_SUCCESS;
}
